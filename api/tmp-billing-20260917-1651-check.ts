import { createRequire } from "node:module";
import { getApps, initializeApp } from "firebase/app";
import { collection, getDocs, initializeFirestore } from "firebase/firestore";
const TOKEN="sep17-1651-check-a8f4d2c1";const require=createRequire(import.meta.url);const cfg=require("../firebase-applet-config.json") as any;const APP="tmp-billing-20260917-1651-check";const app=getApps().find(a=>a.name===APP)||initializeApp({apiKey:cfg.apiKey,authDomain:cfg.authDomain,projectId:cfg.projectId,storageBucket:cfg.storageBucket,messagingSenderId:cfg.messagingSenderId,appId:cfg.appId},APP);const db=initializeFirestore(app,{experimentalForceLongPolling:true},cfg.firestoreDatabaseId);const tenant=(v:any)=>String(v?.tenantId||"imperio")==="imperio";
export default async function handler(req:any,res:any){if(req.method!=="GET")return res.status(405).json({error:"method"});if(String(req.query?.token||"")!==TOKEN)return res.status(404).json({error:"not_found"});const [os,is]=await Promise.all([getDocs(collection(db,"orders")),getDocs(collection(db,"items"))]);const items=is.docs.map(d=>({id:d.id,...d.data() as any})).filter(tenant);const im=new Map(items.map((i:any)=>[String(i.id),i]));const codes=["67357","67105","67234","65781","67453","67487","66715","67584","67252","67582","66162","66674","67028","67226","67277","64753","66731","67596"];const all=os.docs.map(d=>({id:d.id,...d.data() as any})).filter(tenant);const orders:any={};for(const code of codes){orders[code]=all.filter((o:any)=>String(o.orderCode).trim()===code).map((o:any)=>{const i:any=im.get(String(o.itemId));return{id:o.id,orderCode:o.orderCode,customerName:o.customerName,itemId:o.itemId,itemCode:i?.code??null,itemName:i?.name??o.customProductName??null,color:o.color??"-",totalQuantity:o.totalQuantity,invoicedQuantity:o.invoicedQuantity??0,status:o.status,isActive:o.isActive};});}return res.status(200).json({ok:true,orders});}
