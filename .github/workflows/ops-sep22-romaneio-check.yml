name: Ops Sep22 Romaneio Check
on:
  push:
    branches: [main]
    paths:
      - 'scripts/ops-sep22-romaneio-check.mjs'
      - '.github/workflows/ops-sep22-romaneio-check.yml'
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci --ignore-scripts
      - run: node scripts/ops-sep22-romaneio-check.mjs
