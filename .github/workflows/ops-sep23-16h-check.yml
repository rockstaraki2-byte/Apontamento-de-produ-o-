name: Ops Sep23 16h Billing Check
on:
  push:
    branches: [main]
    paths:
      - 'scripts/ops-sep23-16h-check.mjs'
      - '.github/workflows/ops-sep23-16h-check.yml'
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: node scripts/ops-sep23-16h-check.mjs
