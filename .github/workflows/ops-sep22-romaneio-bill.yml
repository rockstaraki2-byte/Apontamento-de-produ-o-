name: Ops Sep22 Romaneio Billing
on:
  push:
    branches: [main]
    paths:
      - 'scripts/ops-sep22-romaneio-bill.ts'
      - '.github/workflows/ops-sep22-romaneio-bill.yml'
jobs:
  bill:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx tsx scripts/ops-sep22-romaneio-bill.ts
